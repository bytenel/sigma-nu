<% content_for :main_content do %>

<div class="module">
  <h3><%= action_name.humanize %> Post</h3>
    <div class="module_body">
      <%= form_for [@postable, @post] do |f| %>
        <%= @post.formatted_errors %>
        <%= f.label :body %><br />
        <%= f.text_area :body, :cols => 60, :rows => 15 %>
    </div>
    <div class="module_footer">
     <%= f.submit "Submit", :class => "btn" %>
     <%= link_to "Cancel", :back %>
    </div>
    <% end %>
  </div>

<% end %>